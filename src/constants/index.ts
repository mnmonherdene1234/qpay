/**
 * QPay API Constants
 */

/** QPay API Host URL */
export const QPAY_HOST = "https://merchant.qpay.mn";

/** API Version */
export const API_VERSION = "v2";

/** Token expiry buffer (2 hours in milliseconds) */
export const TOKEN_EXPIRY_BUFFER_MS = 2 * 60 * 60 * 1000;
